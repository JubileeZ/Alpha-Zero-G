# Device Setup layering: lean EP; judge/orchestrate deferred — Candidate killed

Always-on Device Setup stays **lean Execution Protocol** (EP v1 live, ADR 0016). Full fraud-hunt / multi-agent fan-out stay **out of always-on** (on-demand skills only if re-earned later via Trap). Default non-trivial work = core EP — not auto-Orchestrate. Ship names if ever shipped: `judge` / `orchestrate` (no `fable-*`).

**Status:** accepted 2026-08-09; **superseded in part 2026-08-09** — EP v2 Candidate (`templates/candidates/execution-protocol-v2/`) **killed** after Preview (skills pack lost; clean-slate beat Cur on one seed but s9 still soft / ranking incomplete). Operator chose **keep Current EP v1**; pack + stager + wip v2 **deleted**. Layering rule (no auto-Orchestrate / no fraud catalogue in always-on) remains.

**Considered options:** paste fraud-hunt into always-on (rejected); auto-Orchestrate (rejected); Candidate with skills (Preview lost); instructions-only EP v2 (Preview mixed — **not adopted**).

**Consequences:** Candidate slot empty. Device Setup = ADR 0016. Research retained: `docs/research/2026-08-09-auditor-orchestrator-always-on.md`.

**Amends:** ADR 0010; ADR 0016 (v2 path abandoned — stay on promoted v1).
