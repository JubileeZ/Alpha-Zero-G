# Device Setup layering: EP always-on; judge + orchestrate on-demand

Always-on Device Setup stays **lean Execution Protocol** (verify-by-observation, owed `INTENT`/`AUTH`/`PENDING`/`TWINS` artifacts). Full **fraud-hunt** lives in on-demand **`judge`** skill; multi-agent fan-out lives in on-demand **`orchestrate`** skill. Default non-trivial work = core EP — **not** auto-Orchestrate after triviality. Escalate: user invoke (`/judge`, `/orchestrate`) **or** high bar (multi-area evidence · long unattended/multi-phase · large blast radius). Conditional fresh-context verify subagent / `judge` when consequential — not every edit. Ship names: **`judge`**, **`orchestrate`** only (no `fable-*` on device). Credit Sahir619/fable-method in Candidate README/NOTICE; Trap Process Gate (ADR 0012) remains sole promote path.

**Status:** accepted 2026-08-09

**Considered options:** paste full Hunt-classic-frauds into always-on Step 5 (rejected — adherence + laundry-list); auto-Orchestrate for all non-trivial (rejected — ~15× token multi-agent; coding poor fit); skills-only Candidate without EP escalate pointer (rejected — Q7 same-wave packaging).

**Consequences:** Candidate pack `templates/candidates/execution-protocol-v2/` carries EP v2 text + `skills/judge` + `skills/orchestrate` until Trap promote into `templates/global/`. EP v2 must not fold fraud catalogue or auto-Orchestrate into always-on without new Trap + ADR. Research: `docs/research/2026-08-09-auditor-orchestrator-always-on.md`.

**Amends:** ADR 0010 (post-0016 reality: orchestrate/judge not in global; Candidate may re-earn); ADR 0016 (v2 path constrained by this layering).
