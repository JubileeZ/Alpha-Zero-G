# Distilled intent-gates for device defaults

Device defaults will **not** vendor Sahir619/fable-method. Candidate change is an **azg-owned** distillation of forced-artifact thinking gates into `AZG:AGENT-INSTRUCTIONS` (rendered to Cursor `azg-agent-instructions.mdc` + Gemini `AGENTS.md`). `PONYTAIL:MANAGED` stays a pure upstream mirror — never splice gates there (`azg update --vendor` would wipe them).

**In scope for the Candidate text:** triviality gate · ask-classify · define-done · `INTENT:` / `AUTH:` / `TWINS:` / `PENDING:` · report sweep · **expanded verify** (done criterion observed + surrounding tests/build/lint for touched area + `TWINS:` when a defect was fixed). AUTH = outward/irreversible only; missing quote → `PENDING` and continue (no whole-loop halt). Ponytail wins on diff size / efficient laziness (lazy ≠ poor); gates win on ask-shape, done, and forced report lines. No “fable” product name on device; ADR cites fable-method MIT ~v1.4.0 as provenance only. Upstream: neither pin nor track.

**Out (original):** four fable skills as packages · domain adapters · `references/` · upstream portable `AGENTS.md` · third vendor bundle.

**Amended by ADR 0010:** Prove stance + fit/recall crumbs in always-on; Device Setup ships research/data-analysis Domain Adapter Skills + method-refs (Act/orchestrate skill deferred — not shipped). Still no full Fable vendor paste / domain maker.

**Adopt iff** Trap Suite Process Gate promote (ADR 0012): Candidate pass rate ≥ Current and ≥ Baseline **and** `isolation=docker`. Fail → stay deferred (no opt-in). Costume / step-header leakage / sandbox → watch-notes only, not hard reject. Map: [#75](https://github.com/JubileeZ/alpha-zero-g/issues/75). Prior SWE-bench Lite path (ADR 0007) superseded 2026-08-07.

**Status:** **Parked / clean slate 2026-08-07** — Fable-distilled always-on removed from `AZG:AGENT-INSTRUCTIONS` (kept Temporary File Cleanup + Telegraphic only). Azg distill skills **deleted** from `templates/global/skills/azg/` (re-earn via Trap Suite). Prior Lite adopt (2026-08-01) historical. Map [#85](https://github.com/JubileeZ/alpha-zero-g/issues/85) · [#90](https://github.com/JubileeZ/alpha-zero-g/issues/90).
