# Think/Prove Candidate: crumbs first, Ponytail last, family skills

Re-earn distill after clean slate. Always-on `AZG:AGENT-INSTRUCTIONS` holds lean Think/Prove crumbs + precedence header (process owns whether/order/artifacts; Ponytail owns how-much-code after evidence). Template + `azg setup` sync put **AGENT-INSTRUCTIONS before `PONYTAIL:MANAGED`**; owned installs migrate order (in-place content sync alone kept Ponytail-first). `PONYTAIL:MANAGED` stays pure upstream mirror — never splice gates there.

Ship azg-owned family pack under `templates/global/skills/azg/`: `azg-orchestrate`, `azg-method-refs` (+ failure-modes ref), eight Domain Adapter Skills (research, data-analysis, marketing, business-ops, finance, legal, design, devops). Fresh writing-for-agents / telegraphic distill from Fable Method MIT ideas — no upstream paste, no “fable” product name on device. Setup installs azg skills when dir present. Trap eval stages skills only with `AZG_EVAL_AZG_SKILLS=1`.

**Bar this Candidate:** beat Current on Trap Process Gate (ADR 0012). Longer goal: match/beat upstream Fable pack.

**Status:** accepted (Candidate). Promote only via Trap + `isolation=docker`.

**Supersedes:** ADR 0009 (parked clean-slate text), ADR 0010 (two-domain / orchestrate-deferred design).

**Amends:** ADR 0008 Cursor rule source — block order now AGENT-INSTRUCTIONS then Ponytail.
