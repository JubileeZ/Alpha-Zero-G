# Domain adapter: TEMPLATE

Schema for domain adapters. Adapters change nouns (evidence, authority, verification, frauds) while preserving the loop. Target length: 30-45 lines. Medical/clinical work explicitly excluded (requires qualified review).

---

# Domain adapter: <sector>

Applies when deliverable is <actual outputs>. The loop is unchanged; definitions replace coding defaults. <Boundary sentence naming nearest adapter/coding default.>

## Workflow (steps + flowchart)

```mermaid
flowchart TD
    <domain steps as decision/action nodes>
```

## Minimum evidence set (binding, before action)

1. **<Governing doc/ground truth>**: <what must be opened>.
2. **<Subject's facts>**: <primary material claims trace to>.
3. **<Live external reference>**: <fetched now, not recalled>.

## Evidence and primary sources

<What counts as primary source vs signature non-evidence.>

## Authority order

<Ordered chain using ">", from user instruction down to preference. Classic conflict resolved.>

## Verification by observation

- <What observed means; checks run; exactness rules.>

## Fraud table

| Fraud | Symptom |
|---|---|
| <Name> | <Observable symptom for judge diff/re-run> |

## Done, by example

"<Deliverable> is done" means: <observed checklist>. Not: "<hollow claim>".

## Sources

- <Regulation/policy/link + access date>
