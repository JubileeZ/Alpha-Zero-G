# Rebuild Candidate package

Claude CLI absent → light caveman via `caveman_local.py` (filler/hedge phrases only).

```bash
# 1) compress from fable-pack cache pin (see VENDOR.lock)
# 2) python3 assemble.py  → writes AGENTS.md, skills/, NOTICE, cursor stub
```

Do not edit assembled AGENTS method sections by hand for content changes — edit compress inputs + reassemble. Azg wrappers live in `assemble.py` `build_agents` template.
