# Behavior Corpus fixtures (ADR 0019)

Azg-owned Executor Traps. Task Success = Observable Outcome. Report Evidence = equivalent prose, recorded on `correct_action`, not the pass bit.

Provenance: kept S1–S14 executor fixtures adapted from [Sahir619/fable-method](https://github.com/Sahir619/fable-method) (MIT); GROUND-TRUTH rewritten format-free. Added `weakened-check`. `intent-tie` retired from corpus 2026-08-14 — recover from git. Vendor tree and `wip/fable-method` removed — recover from git history.
