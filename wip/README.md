# WIP (tracked work in progress)

Git-tracked scratch for candidate handcraft. **Not** session temp debris; do not delete during agent cleanup.

| Path | Purpose |
|------|---------|
| `fable-method/compressed/` | Upstream-shaped reference compress (credit when deriving; not Device Setup) |

Promote flow: `templates/candidates/<pack-id>/` → Trap gate → `templates/global/` (ADR 0012).
